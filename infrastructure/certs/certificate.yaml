apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: homelab-tls-cert
  namespace: traefik-system
spec:
  secretName: homelab-tls
  issuerRef:
    name: home-ca-issuer
    kind: ClusterIssuer
  dnsNames:
    - "*.dc.home"
    - "dc.home"
